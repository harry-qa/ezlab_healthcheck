# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ezlab.spec.ts >> 이지랩 서비스 통합 점검 (서버 / API / UI)
- Location: tests/ezlab.spec.ts:58:5

# Error details

```
Error: [UI][ko] 정상 → https://ezlab.im/ko/tool/ezmanager

expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

```
Error: [UI][ko] 정상 → https://ezlab.im/ko/tool/eznote

expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

```
Error: [UI][ko] 리다이렉트 → https://ezlab.im/ko/tool/eznote → https://ezlab.im/tool/eznote

expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e2]:
    - banner [ref=e3]:
      - generic [ref=e4]:
        - heading [level=1] [ref=e5]:
          - link [ref=e6] [cursor=pointer]:
            - /url: /tw/
            - img [ref=e9]
        - navigation [ref=e10]:
          - list [ref=e11]:
            - listitem [ref=e12]:
              - button "使用評價" [ref=e13] [cursor=pointer]
          - button "CHN" [ref=e18] [cursor=pointer]:
            - img [ref=e20]
            - generic [ref=e24]: CHN
            - img [ref=e26]
    - main [ref=e28]:
      - generic [ref=e29]:
        - generic [ref=e33]:
          - generic [ref=e34]:
            - img "ezDown" [ref=e37]
            - heading "不受廣告干擾， 盡情享受影片的本質。" [level=1] [ref=e38]:
              - generic [ref=e39]: 不受廣告干擾，
              - generic [ref=e40]: 盡情享受影片的本質。
            - button "Get it on Google Play" [ref=e42] [cursor=pointer]:
              - img "Get it on Google Play" [ref=e43]
          - img "ezDown" [ref=e46]
        - generic [ref=e49]:
          - generic [ref=e51]: Features
          - heading "輕鬆快速的 YouTube 播放體驗" [level=2] [ref=e53]
          - paragraph [ref=e54]: 乾淨地享受您喜愛的 YouTube 影片。
          - generic [ref=e60]:
            - generic [ref=e61]:
              - generic [ref=e63]: 廣告封鎖
              - generic [ref=e65]:
                - img "廣告封鎖" [ref=e67]
                - paragraph [ref=e68]: 封鎖 YouTube 影片廣告，提供舒適的觀看環境。
            - generic [ref=e71]: 背景播放
            - generic [ref=e74]: 個人化推薦影片
        - generic [ref=e76]:
          - generic [ref=e78]:
            - paragraph [ref=e79]: The Standard of Excellence
            - heading "享受影片的全新標準" [level=2] [ref=e80]
          - generic [ref=e81]:
            - generic [ref=e83]:
              - generic [ref=e85]: Coming Soon
              - img [ref=e87]
              - heading "安心給孩子使用的兒童模式" [level=4] [ref=e90]
              - paragraph [ref=e91]: 就像 YouTube Kids！ 只顯示適齡內容，讓您放心觀看。
              - generic [ref=e92]: 功能即將推出
            - generic [ref=e94]:
              - img [ref=e96]
              - heading "一覽個人化推薦與熱門內容" [level=4] [ref=e99]
              - paragraph [ref=e100]: 在同一畫面確認 YouTube 個人化推薦影片與熱門內容。
            - generic [ref=e102]:
              - img [ref=e104]
              - heading "裝置內的影片・音樂，立即播放！" [level=4] [ref=e107]
              - paragraph [ref=e108]: 本機檔案也能透過離線播放器輕鬆欣賞。
            - generic [ref=e110]:
              - img [ref=e112]
              - heading "內部・外部 PIP 不中斷觀看" [level=4] [ref=e115]
              - paragraph [ref=e116]: 切換至其他應用程式時，自動轉換為外部子母畫面。
        - generic [ref=e118]:
          - heading "用戶真實評價" [level=2] [ref=e120]
          - generic [ref=e122]:
            - generic [ref=e123]:
              - generic [ref=e124]:
                - img [ref=e125]
                - img [ref=e127]
                - img [ref=e129]
                - img [ref=e131]
                - img [ref=e133]
              - paragraph [ref=e135]: “無廣告看影片，這才是重點！”
              - paragraph [ref=e136]: 沒有廣告、播放清爽，真的很棒。現在誰還在看廣告呢。
              - generic [ref=e137]:
                - generic [ref=e138]: MJ
                - generic [ref=e139]:
                  - paragraph [ref=e140]: mj*****
                  - paragraph [ref=e141]: Verified User
            - generic [ref=e142]:
              - generic [ref=e143]:
                - img [ref=e144]
                - img [ref=e146]
                - img [ref=e148]
                - img [ref=e150]
                - img [ref=e152]
              - paragraph [ref=e154]: “PIP 切換流暢，多語言支援也很讚！”
              - paragraph [ref=e155]: 多語言支援讓我很容易找到選單，從 App 內 PIP 切換到外部 PIP 也非常自然。
              - generic [ref=e156]:
                - generic [ref=e157]: DH
                - generic [ref=e158]:
                  - paragraph [ref=e159]: dh****
                  - paragraph [ref=e160]: Verified User
            - generic [ref=e161]:
              - generic [ref=e162]:
                - img [ref=e163]
                - img [ref=e165]
                - img [ref=e167]
                - img [ref=e169]
                - img [ref=e171]
              - paragraph [ref=e173]: “播放清單管理很方便”
              - paragraph [ref=e174]: 可以在播放器中繼續觀看影片，這點很好。螢幕關閉也持續播放，所以我常拿來當音樂播放器用。
              - generic [ref=e175]:
                - generic [ref=e176]: YK
                - generic [ref=e177]:
                  - paragraph [ref=e178]: yk****
                  - paragraph [ref=e179]: Verified User
        - generic [ref=e182]:
          - generic [ref=e184]:
            - heading "常見問題" [level=2] [ref=e185]
            - paragraph [ref=e187]: 了解 ezDown 的使用方式與常見疑問。
          - generic [ref=e189]:
            - generic [ref=e190]:
              - button "提供哪些功能？" [ref=e191] [cursor=pointer]:
                - generic [ref=e192]: 提供哪些功能？
                - img [ref=e194]
              - paragraph [ref=e196]: 提供 YouTube 廣告封鎖、高畫質觀看、PIP 子母畫面等功能。
            - generic [ref=e197]:
              - button "需要登入 YouTube 帳號嗎？" [ref=e198] [cursor=pointer]:
                - generic [ref=e199]: 需要登入 YouTube 帳號嗎？
                - img [ref=e201]
              - paragraph [ref=e203]: 一般影片觀看無需登入即可使用。會員專屬內容、個人播放清單、觀看紀錄同步則可能需要 YouTube 帳號驗證。
            - generic [ref=e204]:
              - button "支援深色模式嗎？" [ref=e205] [cursor=pointer]:
                - generic [ref=e206]: 支援深色模式嗎？
                - img [ref=e208]
              - paragraph [ref=e210]: 是的，您可以在「我的頁面 > 設定」中選擇跟隨系統設定，或個別選擇深色/淺色主題。
            - generic [ref=e211]:
              - button "支援背景播放嗎？" [ref=e212] [cursor=pointer]:
                - generic [ref=e213]: 支援背景播放嗎？
                - img [ref=e215]
              - paragraph [ref=e217]: 是的，在行動裝置上，即使螢幕關閉或使用其他應用程式，仍可播放影片。
            - generic [ref=e218]:
              - button "可以語音搜尋嗎？" [ref=e219] [cursor=pointer]:
                - generic [ref=e220]: 可以語音搜尋嗎？
                - img [ref=e222]
              - paragraph [ref=e224]: 是的，支援語音辨識功能，將語音轉換為搜尋關鍵字，並找到相符的影片。
            - generic [ref=e225]:
              - button "有自動播放功能嗎？" [ref=e226] [cursor=pointer]:
                - generic [ref=e227]: 有自動播放功能嗎？
                - img [ref=e229]
              - paragraph [ref=e231]: 是的，開啟自動播放後，一部影片結束 10 秒後，下一部影片將自動播放。
            - generic [ref=e232]:
              - button "如何退出會員？" [ref=e233] [cursor=pointer]:
                - generic [ref=e234]: 如何退出會員？
                - img [ref=e236]
              - paragraph [ref=e238]:
                - text: 可在
                - link "會員退出" [ref=e239] [cursor=pointer]:
                  - /url: /tw/withdrawal/
                - text: 頁面進行。提交退出申請後，將由管理員審核並完成退出。
    - contentinfo [ref=e240]:
      - generic [ref=e242]:
        - generic [ref=e243]:
          - generic [ref=e244]:
            - generic [ref=e245]: ezDown Lite
            - paragraph [ref=e246]: "Contact Us: ezdown26@gmail.com"
          - generic [ref=e248]:
            - link "使用條款" [ref=e249] [cursor=pointer]:
              - /url: /tw/term/use/
            - link "隱私權政策" [ref=e250] [cursor=pointer]:
              - /url: /tw/term/privacy/
        - generic [ref=e251]:
          - paragraph [ref=e252]: © 2026 ezdown. Premium Editorial Design.
          - generic [ref=e255]: All rights reserved
    - button "맨 위로 이동":
      - img
  - alert [ref=e256]
```

# Test source

```ts
  228 |       const buf = await page.screenshot({ path: filePath, fullPage: true });
  229 |       await test.info().attach(label, { body: buf, contentType: 'image/png' });
  230 |       shotCount++;
  231 |       console.log(`[SCREENSHOT] ${label} → ${filePath}`);
  232 |     } catch (e) {
  233 |       console.log(`[SCREENSHOT] 캡처 실패(${label}): ${(e as Error).message}`);
  234 |     }
  235 |   }
  236 | 
  237 |   async function checkUrl(type: string, lang: string, url: string, isInternal: boolean = true) {
  238 |     const cleanUrl = url.split('?')[0];
  239 |     if (visitedUrls.has(cleanUrl)) return;
  240 |     visitedUrls.add(cleanUrl);
  241 | 
  242 |     // 내부/외부 공통으로 쿼리 파라미터 포함 원본 URL로 요청한다.
  243 |     // 내부 링크에서 쿼리를 떼면(과거 동작) /ko/tool/view?id=123 이 /ko/tool/view 로 요청돼
  244 |     // 파라미터 필수 페이지가 404/500을 내며 오탐 FAIL이 났다 → STEP2 fetchUrl 처리와 동일하게 원본 유지.
  245 |     // (dedup·표시는 cleanUrl 유지)
  246 |     const requestUrl = url;
  247 | 
  248 |     // 내부 링크는 오리진 순간 지연(캐시 미적용 SSR 페이지의 간헐 타임아웃/5xx/404) 오탐을 막기 위해
  249 |     // 짧은 백오프로 재확인한다. 외부 링크는 WARN이라 지연 누적 방지를 위해 1회만 요청.
  250 |     // (아래 catch에서도 시도 횟수·실패 내역을 증상에 써야 하므로 try 바깥에 둔다)
  251 |     const maxAttempts = isInternal ? 3 : 1;
  252 |     const failureLog: string[] = []; // 실패한 시도의 상태·소요 시간 — 회복 기록의 원인 추적용
  253 | 
  254 |     try {
  255 |       await page.waitForTimeout(Math.floor(Math.random() * 500) + 300);
  256 | 
  257 |       let res!: Awaited<ReturnType<typeof page.request.get>>;
  258 |       let responseTime = 0;
  259 |       let attempts = 0;
  260 |       for (let attempt = 1; attempt <= maxAttempts; attempt++) {
  261 |         attempts = attempt;
  262 |         const startTime = Date.now();
  263 |         try {
  264 |           res = await page.request.get(requestUrl, { headers, timeout: REQUEST_TIMEOUT_MS });
  265 |           responseTime = Date.now() - startTime;
  266 |           if (res.status() === 200 || attempt >= maxAttempts) break; // 성공 또는 마지막 시도 → 아래에서 판정
  267 |           failureLog.push(`${attempt}회차 HTTP ${res.status()} (${responseTime}ms)`);
  268 |         } catch (e) {
  269 |           failureLog.push(`${attempt}회차 응답 없음 (${Date.now() - startTime}ms)`);
  270 |           if (attempt >= maxAttempts) throw e; // 마지막 시도까지 실패 → 바깥 catch(접속 불가)로
  271 |         }
  272 |         await page.waitForTimeout(1500 * attempt); // 1.5s → 3s 백오프 (오리진 회복 대기)
  273 |       }
  274 | 
  275 |       const status = res.status();
  276 |       const finalUrl = res.url();
  277 |       const recovered = attempts > 1 && status === 200; // 재시도로 회복된 간헐 케이스
  278 |       // 요청한 URL(외부 링크는 쿼리 포함 원본)과 비교 — cleanUrl과 비교하면 쿼리가 있는
  279 |       // 외부 링크가 리다이렉트 없이도 전부 [REDIRECT]로 오표기된다.
  280 |       const isRedirect = finalUrl !== requestUrl;
  281 | 
  282 |       let contentIssue = '';
  283 |       // 콘텐츠(점검중) 스캔은 내부 링크에만 적용한다. 외부 링크는 이지랩 소유가 아니라 정책상 WARN까지만인데,
  284 |       // contentIssue는 200이어도 FAIL로 승격시켜(아래 result) 외부 파트너/스토어 페이지에 '점검중' 문구가
  285 |       // 있으면 이지랩 장애로 오탐 + Slack·이슈까지 울렸다 → 내부일 때만 검사해 외부=WARN 정책을 지킨다.
  286 |       if (status === 200 && isInternal) {
  287 |         // 원문 그대로 스캔하면 인라인 <script>/<style>에 실린 i18n JSON 번들(점검 문구 포함)까지
  288 |         // 걸려 정상 페이지가 전량 오탐 FAIL 날 수 있다(STEP5/8은 innerText만 봄). 스크립트·스타일
  289 |         // 블록을 걷어내 '실제 렌더될 마크업'에 가깝게 만든 뒤 검사한다.
  290 |         const body = (await res.text())
  291 |           .replace(/<script[\s\S]*?<\/script>/gi, '')
  292 |           .replace(/<style[\s\S]*?<\/style>/gi, '')
  293 |           .toLowerCase();
  294 |         const found = errorKeywords.find(kw => body.includes(kw.toLowerCase()));
  295 |         if (found) contentIssue = `콘텐츠 이상: "${found}" 감지`;
  296 |       }
  297 | 
  298 |       const isSlow = responseTime > SLOW_MS;
  299 |       // 내부 링크의 실패 기준을 apiIsFail(404·5xx)과 통일한다. 401/403은 비로그인 헬스체크에서
  300 |       // 인증 게이트일 뿐 장애가 아니므로(STEP2도 동일 정책) 내부라도 FAIL로 보지 않는다.
  301 |       const authGated = status === 401 || status === 403;
  302 |       // 외부 링크 이상은 INFO(정보성) — 이지랩이 소유·수정할 수 없는 제3자 서버라 헬스 스코어에서 뺀다.
  303 |       // (abr.ge 등 외부 도메인의 500/504가 이지랩 점수를 깎던 문제. 링크 유효성 기록 자체는 그대로 남는다)
  304 |       const result =
  305 |         status !== 200 ? (isInternal ? (apiIsFail(status) ? 'FAIL' : 'PASS') : 'INFO') :
  306 |         contentIssue   ? 'FAIL' :
  307 |         isSlow         ? 'SLOW' : 'PASS';  // 내부/외부 공통: 느림은 정보성(SLOW) — 스코어 미반영
  308 | 
  309 |       const notes: string[] = [];
  310 |       if (isRedirect)   notes.push(`리다이렉트 → ${finalUrl}`);
  311 |       // 200이 아닌데 PASS로 흡수된 경우(401/403 등)는 로그에서 '정상'으로만 보이지 않도록 근거를 남긴다.
  312 |       if (status !== 200 && result === 'PASS') notes.push(`HTTP ${status}${authGated ? ' (인증 게이트 — 정상)' : ' (비장애 상태코드)'}`);
  313 |       if (contentIssue) notes.push(contentIssue);
  314 |       // 3~12s는 SLOW로 흡수하되, 8s를 넘는 건 오리진 장애 전조라 로그에서 눈에 띄게 구분한다.
  315 |       if (isSlow)       notes.push(`${responseTime > VERY_SLOW_MS ? '응답 매우 느림' : '응답 느림'}: ${responseTime}ms`);
  316 |       if (recovered)    notes.push(`간헐 회복 (${attempts}회 시도${failureLog.length ? ' · ' + failureLog.join(', ') : ''})`);
  317 |       if (!isInternal)  notes.push('외부 링크');
  318 |       const noteStr = notes.join(' | ') || '정상';
  319 | 
  320 |       if (result === 'FAIL') {
  321 |         failCount++;
  322 |         const ts = kstNow();
  323 |         console.log(`[FAIL][${type}][${lang}] ${status} (${responseTime}ms) ${cleanUrl} @ ${ts}`);
  324 |         failRecords.push({ step: 'UI/링크', type, lang, url: cleanUrl, status, responseTime, symptom: noteStr, timestamp: ts });
  325 |         // status가 아니라 '판정 결과'를 단언한다. status로 단언하면 HTTP 200인데 점검 페이지가
  326 |         // 내려온 경우(contentIssue)에 리포트는 FAIL인데 assert는 통과해, CI가 초록불이 되는
  327 |         // 정합성 구멍이 생긴다.
> 328 |         expect.soft(status === 200 && !contentIssue, `[${type}][${lang}] ${noteStr} → ${cleanUrl}`).toBe(true);
      |                                                                                                     ^ Error: [UI][ko] 리다이렉트 → https://ezlab.im/ko/tool/eznote → https://ezlab.im/tool/eznote
  329 |       } else if (result === 'SLOW') {
  330 |         slowCount++;  // 정보성 집계만 — 런 상태(WARN)/헬스 스코어를 깎지 않고 CI도 실패시키지 않음
  331 |         console.log(`[SLOW][${type}][${lang}] ${status} (${responseTime}ms) ${cleanUrl}`);
  332 |       } else if (result === 'INFO') {
  333 |         // 스코어엔 안 넣되 기록은 반드시 남긴다 — 외부 링크가 죽은 게 이지랩 장애는 아니어도
  334 |         // 사용자는 깨진 링크를 밟게 되므로, 링크를 교체·제거할 근거는 대시보드에 남아야 한다.
  335 |         infoCount++;
  336 |         const ts = kstNow();
  337 |         console.log(`[INFO][${type}][${lang}] ${status} (${responseTime}ms) [외부링크 · 스코어 미반영] ${cleanUrl}`);
  338 |         failRecords.push({ step: 'UI/링크', type: '외부링크', lang, url: cleanUrl, status, responseTime, symptom: `외부 링크 응답 이상 (HTTP ${status})`, timestamp: ts, severity: 'INFO' });
  339 |       } else {
  340 |         passCount++;
  341 |         console.log(`[PASS][${type}][${lang}] ${status} (${responseTime}ms)${isRedirect ? ' [REDIRECT]' : ''} ${cleanUrl}`);
  342 |       }
  343 | 
  344 |     } catch (e) {
  345 |       if (isInternal) {
  346 |         failCount++;
  347 |         const ts = kstNow();
  348 |         // '접속 불가'로만 적으면 실제로는 연결이 살아 있고 응답만 느렸던 경우까지 단절로 오기재된다.
  349 |         // 타임아웃 값을 명시해 '얼마를 기다렸는데 안 왔는지'를 증상에 남긴다.
  350 |         const symptom = `응답 없음 (${maxAttempts}회 시도 · 각 ${REQUEST_TIMEOUT_MS / 1000}초 대기)${failureLog.length ? ' · ' + failureLog.join(', ') : ''}`;
  351 |         console.log(`[ERROR][${type}][${lang}] ${symptom}: ${cleanUrl} @ ${ts}`);
  352 |         failRecords.push({ step: 'UI/링크', type, lang, url: cleanUrl, status: 0, responseTime: 0, symptom, timestamp: ts });
  353 |         expect.soft(null, `[${type}][${lang}] ${symptom} → ${cleanUrl}`).not.toBeNull();
  354 |       } else {
  355 |         // 외부 도메인 접속 불가도 위 응답 이상과 동일하게 INFO — 제3자 서버 상태는 스코어에 반영하지 않는다.
  356 |         infoCount++;
  357 |         const ts = kstNow();
  358 |         console.log(`[INFO][${type}][${lang}] 외부링크 접속 불가 (스코어 미반영): ${cleanUrl}`);
  359 |         failRecords.push({ step: 'UI/링크', type: '외부링크', lang, url: cleanUrl, status: 0, responseTime: 0, symptom: '외부 링크 접속 불가 / 타임아웃', timestamp: ts, severity: 'INFO' });
  360 |       }
  361 |     }
  362 |   }
  363 | 
  364 |   // ══════════════════════════════════════════════════════════════════
  365 |   // STEP 1: 다국어 서버 생존 확인
  366 |   // ══════════════════════════════════════════════════════════════════
  367 |   await test.step('STEP 1 · 다국어 서버 생존 확인 (ko / en / jp / tw)', async () => {
  368 |     // 언어 간 연결을 재사용하는 keep-alive 에이전트 (전부 동일 호스트라 핸드셰이크 1회면 충분).
  369 |     const agent = new https.Agent({ keepAlive: true, maxSockets: 1 });
  370 |     try {
  371 |       for (const lang of languages) {
  372 |         // STEP1·2도 예산 가드를 둔다. 오리진 지연으로 초반 스텝이 밀리면 최종 report-status.json
  373 |         // 저장 전에 전체 타임아웃(10분)에 걸려 런이 UNKNOWN으로 잡히고 Slack 오탐이 난다 —
  374 |         // 사이트는 멀쩡한데 헬스체크가 스스로 장애를 만드는 것. 예산 초과 시 자진 종료해 리포트 저장 보장.
  375 |         if (budgetHit('STEP1·서버')) { console.log('[SKIP][서버] 시간 예산 초과 — 이후 언어 건너뜀'); break; }
  376 |         await test.step(`[서버][${lang}] 응답 확인`, async () => {
  377 |           const serverUrl = `${baseUrl}/${lang}`;
  378 |           visitedUrls.add(serverUrl); // 최종 집계에 포함
  379 |           try {
  380 |             // 워밍업 1회(DNS/TLS·경로 캐시 비용 분리) → 본측정 3회의 중앙값을 TTFB로 채택.
  381 |             await measureTTFB(serverUrl, headers, agent);
  382 |             const samples: number[] = [];
  383 |             const statuses: number[] = [];
  384 |             for (let i = 0; i < 3; i++) {
  385 |               const r = await measureTTFB(serverUrl, headers, agent);
  386 |               statuses.push(r.status);
  387 |               samples.push(r.ttfb);
  388 |             }
  389 |             samples.sort((a, b) => a - b);
  390 |             const responseTime = samples[1]; // 3회 중앙값 (본문 다운로드 제외 TTFB)
  391 | 
  392 |             // 3xx(트레일링 슬래시·정규화 리다이렉트)도 서버 생존으로 간주 — https.get은
  393 |             // 리다이렉트를 안 따라가므로 200만 통과시키면 정상 사이트도 FAIL 오탐이 난다.
  394 |             // 상태는 3회 중 마지막 값만 보면 안 된다(200/200/500이면 blip을 놓치거나 500/500/200이면
  395 |             // 2번 실패를 못 본다) → 하나라도 살아있지 않으면 그 실패 상태를 대표값으로 채택.
  396 |             const alive = (s: number) => s >= 200 && s < 400;
  397 |             const badStatus = statuses.find(s => !alive(s));
  398 |             const status = badStatus ?? statuses[statuses.length - 1];
  399 |             const isAlive = badStatus === undefined;
  400 |             if (isAlive) {
  401 |               passCount++;
  402 |               serverTimes[lang] = responseTime;
  403 |               console.log(`[PASS][${lang}] 서버 ${status} (TTFB ${responseTime}ms · ${samples.join('/')})`);
  404 |             } else {
  405 |               failCount++;
  406 |               const ts = kstNow();
  407 |               console.log(`[FAIL][${lang}] 서버 ${status} (TTFB ${responseTime}ms) @ ${ts}`);
  408 |               failRecords.push({ step: 'STEP1·서버생존', type: '서버', lang, url: serverUrl, status, responseTime, symptom: `HTTP ${status} 응답`, timestamp: ts });
  409 |             }
  410 |             expect.soft(isAlive, `서버 생존 확인 (status: ${status}, url: ${serverUrl})`).toBe(true);
  411 |           } catch {
  412 |             failCount++;
  413 |             const ts = kstNow();
  414 |             console.log(`[ERROR][${lang}] 서버 접속 불가 @ ${ts}`);
  415 |             failRecords.push({ step: 'STEP1·서버생존', type: '서버', lang, url: serverUrl, status: 0, responseTime: 0, symptom: '접속 불가 / 타임아웃', timestamp: ts });
  416 |             expect.soft(null, `접속 불가 (url: ${serverUrl})`).not.toBeNull();
  417 |           }
  418 |         });
  419 |       }
  420 |     } finally {
  421 |       agent.destroy();
  422 |     }
  423 |   });
  424 | 
  425 |   // ══════════════════════════════════════════════════════════════════
  426 |   // STEP 2: API 자동 수집 및 검증 (네트워크 인터셉트)
  427 |   // ══════════════════════════════════════════════════════════════════
  428 |   await test.step('STEP 2 · API 자동 수집 및 검증 (네트워크 인터셉트)', async () => {
```